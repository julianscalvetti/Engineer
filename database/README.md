# Database

- `supabase/schema.sql` es el schema ejecutable actual del MVP.
- `database/domain-model/` contiene el modelo industrial objetivo.
- Ambos no deben sincronizarse automáticamente hasta definir una migración controlada.
